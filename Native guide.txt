================================================================================
SCANCODE MOBILE APP (REACT NATIVE) DEVELOPMENT GUIDE & COMPREHENSIVE ROADMAP
================================================================================

--------------------------------------------------------------------------------
1. MOBILE SCREEN BREAKDOWN & REPOSITORY PARITY COMPARISON
--------------------------------------------------------------------------------
Full Feature Parity Target: 23 Screens
Currently Implemented in Scancode-React-Native: 22 Screens (96%)
Remaining Screens to Build: 1 Screen (4%)

[2026-08-19 corrections, cumulative:
 (1) ToolbarRequestsAdminScreen was already built but untracked — fixed earlier.
 (2) Parity target bumped 22 -> 23: the "Merchant / Admin: 9 Screens" row never actually
     had room for ProductCatalogEditorScreen once all 9 named admin screens existed —
     corrected to 10.
 (3) SuperadminConsoleScreen was counted in the Superadmin summary row but was missing
     from the itemized "Remaining Screens Needed" list since this file's very first
     version — added below for consistency.
 (4) WishlistScreen and CartDrawerScreen promoted from StorefrontScreen modals to real
     dedicated routed screens; OrderReceiptTrackerScreen and EventsManagerScreen built
     (next-two-screens per the phased roadmap). All four now [COMPLETED] below.
 (5) ProductCatalogEditorScreen built (product CRUD, image upload, delist/relist, wired
     into DashboardScreen's per-storefront ops row). Now [COMPLETED] below.
 See AGENTS.md for the full technical change log.]

Summary Comparison Table:
+--------------------------+--------------------+---------------------------+-------------------+
| Category                 | Total Web Parity   | React Native Implemented  | Remaining Needed  |
+--------------------------+--------------------+---------------------------+-------------------+
| Authentication & Account | 5 Screens          | 5 Screens (100%)          | 0 Screens         |
| Customer Storefront      | 7 Screens          | 7 Screens + Toolbar (100%)| 0 Screens         |
| Merchant / Admin         | 10 Screens         | 10 Screens (100%)         | 0 Screens         |
| Superadmin               | 1 Screen           | 0 Screens (0%)            | 1 Screen          |
+--------------------------+--------------------+---------------------------+-------------------+
| TOTAL                    | 23 Screens         | 22 Screens Implemented    | 1 Screen          |
+--------------------------+--------------------+---------------------------+-------------------+

[2026-08-31 — scope beyond the original 23-screen parity target: this session added a
 Services menu, a full Access Page subsystem (event pages, dynamic + preset guest forms,
 guest check-in, exclusive content unlock), and a customer/vendor Storefront Directory
 (sortable by rating/location/alphabetical) — 5 new screens total. These aren't part of
 web-parity tracking (scancode-web has no equivalent), so they're not counted in the table
 above. See "6. POST-PARITY FEATURE ADDITIONS" below.]

Implemented Screens (20):
- Splash (src/screens/(auth)/SplashScreen.tsx)
- Login (src/screens/(auth)/LoginScreen.tsx)
- Register (src/screens/(auth)/RegisterScreen.tsx)
- ForgotPassword (src/screens/(auth)/ForgotPasswordScreen.tsx)
- VerifyOtp (src/screens/(auth)/VerifyOtpScreen.tsx)
- Dashboard (src/screens/(admin)/DashboardScreen.tsx)
- CreateStorefront (src/screens/(admin)/CreateStorefrontScreen.tsx)
- Storefront (src/screens/(customer)/StorefrontScreen.tsx + StorefrontToolbar.tsx)
- Checkout (src/screens/(customer)/CheckoutScreen.tsx)
- ActivateQR (src/screens/(admin)/ActivateQRScreen.tsx)
- QR (src/screens/(admin)/QRScreen.tsx)
- MerchantProfileBank (src/screens/(admin)/MerchantProfileBankScreen.tsx) [COMPLETED]
- StoreChargesConfig (src/screens/(admin)/StoreChargesConfigScreen.tsx) [COMPLETED]
- CameraQRScanner (src/screens/(customer)/CameraQRScannerScreen.tsx) [COMPLETED]
- LiveOrdersManager (src/screens/(admin)/LiveOrdersManagerScreen.tsx) [COMPLETED]
- ToolbarRequestsAdmin (src/screens/(admin)/ToolbarRequestsAdminScreen.tsx) [COMPLETED]
- Wishlist (src/screens/(customer)/WishlistScreen.tsx) [COMPLETED]
- CartDrawer (src/screens/(customer)/CartDrawerScreen.tsx) [COMPLETED]
- OrderReceiptTracker (src/screens/(customer)/OrderReceiptTrackerScreen.tsx) [COMPLETED]
- EventsManager (src/screens/(admin)/EventsManagerScreen.tsx) [COMPLETED]
- ProductCatalogEditor (src/screens/(admin)/ProductCatalogEditorScreen.tsx) [COMPLETED]
- ItemDetailsModal (src/screens/(customer)/ItemDetailsModalScreen.tsx) [COMPLETED 2026-08-31]
  Real @gorhom/bottom-sheet (not a plain Modal) — tap a product card to customize quantity
  and add to cart in one motion, closes out heading 4 item 7 (Native Modal Bottom Sheets)
  at the same time.

Remaining Screens Needed (1):
- SuperadminConsoleScreen (Superadmin dashboard for platform metrics & vendor management —
  deferred; needs a new 'superadmin' appState + navigator + demo account, a real scope
  decision the user chose to skip for this pass)


--------------------------------------------------------------------------------
2. PHASED EXECUTION ROADMAP (SORTED BY SIZE & COMPLEXITY)
--------------------------------------------------------------------------------

PHASE 1: Small & Low Complexity (Quick Wins — Build First)
1. MerchantProfileBankScreen (Small/Low): [COMPLETED] Business bank account details editor.
2. StoreChargesConfigScreen (Small/Low): [COMPLETED] Form with inputs for VAT rate (7.5%), Logistics Fee, and deliveryEnabled toggle.
3. CameraQRScannerScreen (Small/Low-Med): [COMPLETED] Hardware camera integration with expo-camera & fallback simulated scanning.

PHASE 2: Medium Size & Moderate Complexity (Core Functionality)
4. LiveOrdersManagerScreen (Medium): [COMPLETED] Real-time order processing center with Status tabs (Pending/Confirmed/Rejected), Web/Native sound alarms, and action buttons.
5. ToolbarRequestsAdminScreen (Medium): [COMPLETED] Admin view for acknowledging incoming customer tips, paid transfers, and waiter calls.
6. OrderReceiptTrackerScreen (Medium): [COMPLETED] Customer order status screen polling getOrderById every 5s, stops polling once the order reaches a terminal status.
7. EventsManagerScreen (Medium): [COMPLETED] Mon-Sun weekly event schedule manager, CRUD backed by getStorefrontEvents/updateStorefrontEvents, strictly scoped per storefrontId.

Also completed this pass (not originally itemized as their own phase entries, but part of
the same "next two screens" push): WishlistScreen and CartDrawerScreen, promoted from
StorefrontScreen's in-page modals to dedicated routed screens backed by a new shared
CartContext (storefront-scoped cart + favorites).

PHASE 3: Large Size & High Complexity (Advanced Modules — Build Last)
8. ProductCatalogEditorScreen (Large/High): [COMPLETED] Menu CRUD management, stock updates, category tagger, image uploader via expo-image-picker, delist/relist toggle.
9. FrontdeskCheckInScreen (Large/High): Hotel & hospitality guest check-in portal.
10. DigitalSignageExploreScreen (Large/High): Digital promo carousel & venue showcase display.
11. SuperadminConsoleScreen (Large/High): Superadmin dashboard for platform metrics & vendor management.


--------------------------------------------------------------------------------
3. MOBILE ARCHITECTURAL & WEB PARITY DOMAIN GROUPING
--------------------------------------------------------------------------------

1. Web App vs. Mobile App Flow & Grouping Parity:
   - Web App (scancode-web) groups pages under /login, /store/[slug], /checkout, /admin/dashboard, /frontdesk, and /superadmin.
   - Mobile Best Practice structures React Native screen files into domain subfolders:
     * src/screens/(auth)/       <-- Authentication & Onboarding
     * src/screens/(customer)/   <-- Customer Storefront, Checkout & Scanner
     * src/screens/(admin)/      <-- Merchant POS, Live Orders, Charges & Bank Settings

2. Mobile UX Rule of 5 & Task-Based Navigation Grouping: [CHECK]
   Instead of loose screen lists, group merchant services into 4 primary Bottom Navigation Tabs:

   📱 Admin Mobile Navigation Hub (BottomTabNavigator)
   ├── 🛍️ Tab 1: Orders (HIGH FREQUENCY - Real-time Live Operations)
   │    ├── Live Incoming Orders (Confirm / Reject / Cancel)
   │    └── Waiter & Assistance Calls
   │
   ├── 🍔 Tab 2: Catalog (MEDIUM FREQUENCY - Daily Stock & Menu)
   │    ├── Product & Stock Management
   │    ├── Category Tagger
   │    └── Store Gallery Photos
   │
   ├── 🔔 Tab 3: Activity (MEDIUM FREQUENCY - Customer Engagement)
   │    ├── Tip & Request Payment Acknowledgements
   │    ├── Weekly Events Schedule
   │    └── Customer Ratings & Feedback
   │
   └── ⚙️ Tab 4: Settings (LOW FREQUENCY - Setup & Admin)
        ├── VAT Rate % & Delivery Fee Toggle
        ├── Table QR Generator & Activation
        └── Merchant Bank Payout Profile


--------------------------------------------------------------------------------
4. MOBILE-NATIVE UNIQUE SCREENS & HARDWARE FEATURES [CHECK]
--------------------------------------------------------------------------------

1. Native Camera QR & Barcode Scanner (CameraQRScannerScreen): [CORE COMPLETE]
   - Direct integration using 'expo-camera', manual code fallback, auto-navigation to table
     storefronts all built. Haptic-vibration-on-scan and an LED flash toggle were not
     verified/added in the 2026-08-31 pass — not confirmed present.

2. Push Notifications & Background Order Audio Alarms (audioAlert & FCM): [PARTIAL]
   - Push notification CLIENT plumbing (permission + Expo push token registration) built
     2026-08-31 — see AGENTS.md session 7. Foreground order alarms (audioAlert.ts) were
     already built (session 2). Still missing: an actual push-sending backend (none exists
     in this repo) and true background/locked-screen alarm delivery, which needs a backend
     to trigger the push in the first place.

3. ESC/POS Thermal Receipt Printer Integration (PrintReceiptScreen): [NOT STARTED]
   - Deliberately skipped 2026-08-31 (user's call) — needs real printer hardware to
     integrate against and test; building blind against no hardware was judged not worth
     the risk of shipping something untestable.

4. Biometric Quick Login & Staff Switch (LocalAuthScreen): [NOT STARTED]

5. Native Camera & Photo Picker for Menu Editing (expo-image-picker): [COMPLETED]
   - Built as part of ProductCatalogEditorScreen (session 6).

6. Haptic Feedback & Micro-Interactions (expo-haptics): [COMPLETED 2026-08-31]
   - src/utils/haptics.ts (safe no-op on web). Wired into: add-to-cart, favorite toggle,
     category selection, checkout success/failure (StorefrontScreen/CheckoutScreen), and
     order confirm/reject/complete/cancel + sound toggle + test alarm
     (LiveOrdersManagerScreen).

7. Native Modal Bottom Sheets (@gorhom/bottom-sheet): [COMPLETED 2026-08-31]
   - ItemDetailsModalScreen (see Section 1) — real BottomSheetModal with backdrop, not a
     plain Modal. GestureHandlerRootView + BottomSheetModalProvider wired at App.tsx's
     root. Verified live: content, backdrop, and gesture-drag-to-open all work correctly;
     the auto-open spring animation on first present() didn't fire in the web preview
     (a known react-native-web + Reanimated timing quirk) — expected to work correctly on
     native (iOS/Android) where Reanimated's native driver handles it, not verifiable
     further in this environment.

8. Swipe-to-Action Order Cards (SwipeableOrderCard): [NOT STARTED]

9. Offline Mode & Local Storage Sync (SQLite / AsyncStorage): [PARTIAL — COMPLETED 2026-08-31]
   - Scoped to order placement only (the customer-facing action most likely to hit spotty
     venue WiFi), not admin actions. src/utils/offlineQueue.ts: AsyncStorage-backed queue,
     @react-native-community/netinfo detects reconnect and auto-flushes. CheckoutScreen
     queues on a genuine offline failure instead of showing "Order Failed."

10. Geofencing & Location-Based Store Auto-Detection (expo-location): [PARTIAL — COMPLETED 2026-08-31]
    - Deliberately scoped to state-level proximity, not true meters-level geofencing —
      storefronts only capture a Nigerian state (no map/coordinate picker exists), so
      real per-venue geofencing isn't achievable honestly without building that first
      (a real scope decision the user made). src/utils/geoProximity.ts detects the
      customer's nearest state via expo-location + a NIGERIA_STATE_CENTROIDS lookup
      (state-capital coordinates as a stand-in), surfaces a "Near You" sort option on
      StorefrontDirectoryScreen. No background task / TaskManager — foreground-only,
      one-shot detection on screen load.


--------------------------------------------------------------------------------
5. RECOMMENDED MOBILE UX & AESTHETIC ENHANCEMENTS FOR FUTURE IMPLEMENTATION [CHECK]
--------------------------------------------------------------------------------

1. Glassmorphic Cards & Elevated Depth Design System: [NOT STARTED]
   - Implement semi-transparent frosted glass cards (rgba(255, 255, 255, 0.85) with backdropBlur)
     for checkout summaries, order status cards, and float action bars.
   - Use soft dual-tone gradients (#6C63FF to #4F46E5) for active buttons and accent elements.
     Note: this references the old blue primary (#6C63FF) being replaced per item 6 below —
     will need revisiting against whatever green/emerald palette item 6 lands on.

2. High-Contrast OLED Dark Mode for Kitchen & POS Terminals: [COMPLETED 2026-08-31 — scoped]
   - Built as a LiveOrdersManagerScreen-local toggle (Moon/Sun icon in the header), not a
     global app theme — the rest of the app has no dark: variants designed for it yet, so a
     global NativeWind colorScheme switch would have darkened screens never designed for
     it. Uses the exact #09090B / #18181B values specified. If a true app-wide dark mode is
     wanted later, that's a distinct, larger piece of work — flagging rather than silently
     scope-creeping this toggle into one.

3. Micro-Animations & Fluid Spring Transitions: [COMPLETED 2026-08-31]
   - BounceBadge component (src/components/BounceBadge.tsx) — spring pop on cart/favorites
     count change, used in StorefrontScreen. StatusBadge gained the same spring pop on every
     status transition (LiveOrdersManagerScreen, DashboardScreen, etc. — anywhere it's used).
   - Remove coloring from all icons except the toolbar, keep the rest minimalist and monochrome (BW). [NOT STARTED — see note below]
   - make sure everything, from the poages to the icons, buttons etc, are well responsive and screen adaptable, especially for phones capable of doing split-screen and pop-up modes [NOT STARTED — see note below]

4. Skeleton Loading Shimmers: [PARTIAL — COMPLETED 2026-08-31]
   - src/components/Skeleton.tsx (Reanimated pulse). Wired into DashboardScreen,
     StorefrontScreen, and StorefrontDirectoryScreen's loading states — the three highest-
     traffic list-shaped loads. Other screens (CartDrawer, Wishlist, OrderReceiptTracker,
     etc.) still use ActivityIndicator — not swept project-wide this pass.

5. Tactile Haptic Pulses: [COMPLETED 2026-08-31 — see heading 4 item 6 for the full list]
   - Trigger heavy haptic vibration on incoming new order alarms, medium haptic tap on order
     confirmation, and light haptic tick on category selection.

6. Color Scheme: [NOT STARTED — see note below]
   - Scancode's Primary colors are Scanciode green, Emerald and white. Get rid of that blue coloring and adjust the entire application's primary theme, keep in mind the dark mode feature so that both blend seamlessly

[2026-08-31 note: items 3's monochrome-icons/responsive lines and item 6 (color scheme) were
 added directly to this file by the user mid-session, after this session's scoping questions
 had already been asked and answered. Deliberately not attempted blind — see the chat
 response for the questions this needs answered first (exact green/emerald hex values, and
 how far the icon-monochrome pass should reach) before touching tailwind.config.js's
 `primary` color or auditing every icon `color` prop project-wide.]

--------------------------------------------------------------------------------
6. POST-PARITY FEATURE ADDITIONS (beyond the original 23-screen web-parity target)
--------------------------------------------------------------------------------
Added 2026-08-31. These have no equivalent in scancode-web, so they sit outside the
parity table in Section 1 — tracked here instead.

1. ServicesScreen (src/screens/(admin)/ServicesScreen.tsx) [COMPLETED]
   - Entry hub for vendors: "Create Storefront" (no storefronts yet) / "Add Storefront"
     (already has ≥1) card, plus an "Access Page" card. Tapping Access Page with multiple
     storefronts opens an inline picker; with exactly one, it navigates straight through.

2. AccessPageManagerScreen (src/screens/(admin)/AccessPageManagerScreen.tsx) [COMPLETED]
   - Per-storefront event page CRUD. Type picker (Custom / Wedding / Conference-Summit /
     Concert) — the three presets auto-populate a fixed field set (see demoEngine.ts's
     presetFields equivalent in the screen itself); Custom starts blank and fields are
     built one at a time (label, type: text/number/date/yesno/dropdown, required,
     dropdown options). Each page gets a shareable slug, an active/inactive toggle, a
     guest list (expandable, shows submitted responses + check-in time), and delete.

3. AccessPageGuestScreen (src/screens/(customer)/AccessPageGuestScreen.tsx) [COMPLETED]
   - Public, unauthenticated guest-facing screen reached via an access page's slug
     (`AccessPageGuest: { accessPageSlug }`). Renders the page's dynamic form, submits a
     guest entry, then reveals `exclusiveContent` (if set) as the check-in reward.
     Registered in AuthNavigator, AdminNavigator, and CustomerNavigator so it's reachable
     whether or not the visitor has an account — deliberately not gated behind login,
     matching how QR-scan ordering is anonymous-only.

4. StorefrontDirectoryScreen (src/screens/(customer)/StorefrontDirectoryScreen.tsx) [COMPLETED]
   - Post-login landing screen for logged-in customers (CustomerNavigator's initial
     route) and reachable from the Dashboard's Compass icon for vendors. Lists all
     published storefronts (getAllStorefronts), sortable by rating (getStorefrontRatings
     aggregation), location, or alphabetical. When the viewer is a vendor, their own
     storefronts render pinned above the full list. Has its own back button
     (navigation.canGoBack()) since it runs with a hidden native header in both
     navigators it's pushed into.

5. Nigeria location picker (CreateStorefrontScreen.tsx) [COMPLETED]
   - @react-native-picker/picker, backed by NIGERIA_STATES (36 states + FCT) in types.ts.
     Required field; persisted into storefront.data.location, read back via
     parseStorefrontData for both the directory's location sort and card display.

6. demoEngine.ts data layer for all of the above [COMPLETED]
   - Product CRUD (createProduct/updateProduct/deleteProduct), storefront directory
     (getAllStorefronts — published-only, distinct from getMyStorefronts), ratings
     aggregation (getStorefrontRatings, seeded from a new feedbacks store), and full
     Access Page CRUD + guest check-in, all persisted through AsyncStorage with the same
     conventions as the rest of the file (KEYS map, simulateLatency, reset/init coverage).

Not yet built (see AGENTS.md for the live outstanding list):
- Push notification client plumbing (expo-notifications, registerPushToken wiring).
- Terms/Privacy screens + Register checkbox.
- Crash-reporting scaffold (Sentry).
- eas.json build profiles.

================================================================================


In the reactnative project:
Fix the VAT                                                            [DONE 2026-08-19]
Allow Insertion of Custom Notification Tones                           [DONE 2026-08-19]
Ensure weekly events are specific to the Mariketer/storefront that crafted it.  [DONE 2026-08-19]
then make this screen ToolbarRequestsAdminScreen andaffix it accordingly and make the necessary adjustements to the entire project to accomodate the new screen according to the guide but ultimately, according to best practice.  [DONE — built earlier this session]

[2026-08-19 — how each of the above was actually resolved, see AGENTS.md change log for full detail:
 - VAT: StoreConfigResponse.vatRate was stored inconsistently (fraction on save, raw
   percent in seed/fallback data). CheckoutScreen was the one consumer that didn't
   normalize it, so it was computing VAT as 750% of subtotal instead of 7.5% on any
   default/seeded storefront. Fixed the seed/fallback data at the source and added the
   missing normalization guard to CheckoutScreen.
 - Custom Notification Tones: the feature was silently dead because it depended on
   expo-av, which was fully removed from the Expo SDK at version 55 (this project is on
   57). Migrated src/utils/audioAlert.ts to expo-audio; custom tones now play on native
   AND web (expo-audio has real web support, unlike the old setup).
 - Weekly events per storefront: verified (grepped every read/write site) that events
   were already strictly isolated by storefrontId in both the demo layer and the API
   contract — no cross-storefront leakage existed. Built EventsManagerScreen as the
   missing admin CRUD UI on top of that already-correct isolation.]